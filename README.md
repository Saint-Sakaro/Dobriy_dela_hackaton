# Energy of Fun: Hackathon Platform

## Overview
Собираем многостраничный портал на Django + React для поддержки НКО, событий, базы знаний и новостей. На финальном этапе сюда добавим RAG‑систему на GigaChat (пока золочена). За 48 часов закрываем весь CRUD, роли и UX:

- **Федя** — тимлид и backend.
- **Родя** — фронтенд и UI/UX.
- **Сережа** — ML/AI и интеграция RAG.

## Repository Layout
- `backend/` — Django‑проект `core`, DRF API (`accounts`, `organizations`, `events`, `knowledge`, `news`, `assistant`), кастомные разрешения и сиды.
- `frontend/` — React + Vite + shadcn/ui: публичные страницы, личный кабинет, админ‑панель модератора.
- `ml/` — будущая площадка под пайплайн RAG / GigaChat (пока пусто).
- `docs/` — архитектура, выжимка из ТЗ, API‑контракты.
- `requirements.txt` / `package.json` — зафиксированные зависимости.

## Что уже готово
- **API**: кастомный `User`, токен‑авторизация, CRUD для НКО/событий/новостей/материалов, фильтры по городам/категориям, избранное, регистрация на события, VK ID авторизация, сиды `seed_demo`.
- **Frontend**: все страницы подключены к API; избранное, регистрация на события и личный кабинет работают поверх живых эндпоинтов; админ‑панель умеет модерировать и публиковать контент; интеграция Яндекс.Карт на страницах НКО; авторизация через VK ID.
- **Документация**: `docs/architecture.md` (UX‑флоу), `docs/api-contract.md`, `docs/TESTING_GUIDE.md` (инструкция по тестированию), актуальный `README`.
- **В очереди**: интеграция RAG/GigaChat, продакшн‑сборка, CI/CD.

## Local Setup

### Backend
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
# создайте backend/.env (см. пример ниже)
cd backend
python manage.py migrate
python manage.py seed_demo   # опционально: демо-пользователи/НКО/новости/материалы
python manage.py runserver
```

**Пример `backend/.env`:**
```env
DJANGO_SETTINGS_MODULE=core.settings.dev
SECRET_KEY=dev-secret-change-me
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
CORS_ALLOWED_ORIGINS=http://localhost:5173
# Заглушки под будущий RAG/GigaChat
GIGACHAT_BASE_URL=https://gigachat.api
GIGACHAT_API_KEY=dummy
```

📖 **Подробная инструкция по настройке .env:** см. [`docs/ENV_SETUP.md`](docs/ENV_SETUP.md)

### Frontend
```bash
cd frontend
npm install
echo "VITE_API_BASE_URL=http://localhost:8000/api/v1" > .env.development
npm run dev   # http://localhost:5173
```

Страницы уже подключены к API: города, НКО, события, новости, база знаний, личный кабинет, админка модератора. Для тестов используйте сиды (`demo_user`, `demo_moderator`) или заведите аккаунт через форму регистрации.

**Настройка дополнительных функций:**

1. **Яндекс.Карты** (опционально):
   - Получите API ключ на https://developer.tech.yandex.ru/
   - Замените `YOUR_YANDEX_MAPS_API_KEY` в `frontend/index.html` на реальный ключ
   - Карты будут отображаться на страницах НКО

2. **VK ID авторизация** (опционально):
   - Создайте приложение на https://dev.vk.com/
   - Получите App ID
   - Добавьте в `frontend/.env.development`: `VITE_VK_APP_ID=ваш_app_id`
   - Укажите redirect URL в настройках приложения: `http://localhost:5173/profile`
   - Кнопка "Войти через VK ID" появится в формах авторизации

## 48‑Hour Plan
| Время | Цели | Ответственные |
| --- | --- | --- |
| 0‑6ч | Разобрать ТЗ, описать данные, накидать CJM, финализировать UI‑киты | все |
| 6‑18ч | Django модели, базовые API, мок‑данные; прототип фронта; RAG‑Proof of Concept (пайплайн без GigaChat) | Федя / Родя / Сережа |
| 18‑30ч | Интеграция GigaChat, векторное хранилище (Qdrant/FAISS), авторизация | Сережа / Федя |
| 30‑42ч | UI‑полировка, связывание фронта с API, демо‑сценарии | Родя / Федя |
| 42‑48ч | Тесты, запись видео, репетиция питча, подготовка к сдаче | все |

## Task Board
- **Backend (Федя)**
  - Настроить Django‑проект, окружение, линтеры.
  - Реализовать модели, API, админку.
  - Подготовить endpoints для общения с RAG‑сервисом.
- **Frontend (Родя)**
  - Выбрать стек (Vite+React / Next), настроить дизайн‑систему.
  - Собрать основные страницы и состояния.
  - Подключиться к API + реализовать UX для помощника.
- **ML / AI (Сережа)**
  - Исследовать ТЗ, собрать базу знаний.
  - Настроить пайплайн: эмбеддинги → хранилище → GigaChat.
  - Обернуть RAG в сервис, готовый к вызовам из Django.

## Next Steps
1. Подключить RAG/GigaChat и дописать `assistant.services.RagClient`.
2. Завести CI (lint/test + build) и автодеплой.
3. Добавить end-to-end сценарии (Playwright) и smoke‑тесты API.
4. Полировать UI/UX (Skeletons, уведомления, multi‑language) после основного демо.

