# Energy of Fun: Платформа для волонтёров и НКО

Портал для поддержки некоммерческих организаций, событий, базы знаний и новостей. Разработан на Django REST Framework (backend) и React + Vite (frontend).

## Ссылка на презентацию
https://docs.google.com/presentation/d/1cfMCtkNJFyM0z8-QXqs-2djn4Uff2EGe/edit?usp=sharing&ouid=103650434151574930377&rtpof=true&sd=true

## Ссылка на сайт
https://ct229964.tw1.ru

## 🚀 Быстрый старт

### Требования
- Python 3.9+
- Node.js 18+
- npm или yarn
- Git LFS (для больших файлов)

### Установка Git LFS
```bash
# macOS
brew install git-lfs
git lfs install

# Linux
sudo apt-get install git-lfs
git lfs install

# Windows
# Скачайте установщик с https://git-lfs.github.com/
```

### Backend

1. **Создайте виртуальное окружение:**
```bash
python3 -m venv .venv
source .venv/bin/activate  # На Windows: .venv\Scripts\activate
```

2. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

3. **Настройте переменные окружения:**
Создайте файл `backend/.env`:
```env
DJANGO_SETTINGS_MODULE=core.settings.dev
SECRET_KEY=dev-secret-change-me-in-production
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=sqlite:///db.sqlite3
CORS_ALLOWED_ORIGINS=http://localhost:5173
GIGACHAT_API_KEY=dummy
RAG_SERVICE_URL=http://localhost:9000
```

4. **Примените миграции:**
```bash
cd backend
python manage.py migrate
```

5. **Создайте суперпользователя (опционально):**
```bash
python manage.py createsuperuser
```

6. **Запустите сервер:**
```bash
python manage.py runserver
```

Backend будет доступен по адресу: http://localhost:8000

### Frontend

1. **Установите зависимости:**
```bash
cd frontend
npm install
```

2. **Настройте переменные окружения:**
Создайте файл `frontend/.env.development`:
```env
VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_VK_APP_ID=ваш_vk_app_id  # Опционально, для авторизации через VK ID
```

3. **Запустите dev-сервер:**
```bash
npm run dev
```

Frontend будет доступен по адресу: http://localhost:5173

## 📁 Структура проекта

```
Energy_of_fun/
├── backend/              # Django backend
│   ├── accounts/         # Пользователи и авторизация
│   ├── organizations/    # НКО
│   ├── events/           # События
│   ├── news/             # Новости
│   ├── knowledge/        # База знаний
│   ├── locations/        # Города и категории
│   ├── assistant/        # RAG-ассистент
│   ├── core/             # Настройки проекта
│   └── manage.py
├── frontend/             # React frontend
│   ├── src/
│   │   ├── components/   # React компоненты
│   │   ├── lib/          # API клиент
│   │   └── providers/    # Context провайдеры
│   └── package.json
├── ml/                   # ML модели и RAG система
│   └── data/
│       └── models/       # Модели (требуют Git LFS)
└── requirements.txt      # Python зависимости
```

## 🔑 Основные функции

- **Управление НКО**: создание, редактирование, модерация организаций
- **События**: календарь событий, регистрация, фильтрация
- **Новости**: публикация и просмотр новостей
- **База знаний**: материалы для волонтёров и НКО
- **Личный кабинет**: избранное, регистрации на события, настройки профиля
- **Модерация**: админ-панель для модераторов
- **Поиск**: AI-поиск с использованием RAG системы

## 🛠️ Разработка

### Backend API
API документация доступна после запуска сервера:
- Swagger UI: http://localhost:8000/api/docs/
- ReDoc: http://localhost:8000/api/redoc/

### Frontend
- Dev сервер с hot reload: `npm run dev`
- Production сборка: `npm run build`
- Preview production сборки: `npm run preview`

## 📝 Переменные окружения

### Backend (.env)
- `SECRET_KEY` - секретный ключ Django
- `DEBUG` - режим отладки (1/0)
- `ALLOWED_HOSTS` - разрешённые хосты
- `DATABASE_URL` - URL базы данных
- `CORS_ALLOWED_ORIGINS` - разрешённые источники для CORS
- `GIGACHAT_API_KEY` - API ключ GigaChat (для RAG)
- `RAG_SERVICE_URL` - URL RAG сервиса

### Frontend (.env.development)
- `VITE_API_BASE_URL` - базовый URL API
- `VITE_VK_APP_ID` - ID приложения VK (опционально)

## 🗄️ База данных

По умолчанию используется SQLite. Для продакшена рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
```

## 📦 Зависимости

Все Python зависимости находятся в `requirements.txt`. Для установки:
```bash
pip install -r requirements.txt
```

## 🚢 Деплой

### Backend
1. Установите зависимости
2. Настройте `.env` с продакшн значениями
3. Соберите статику: `python manage.py collectstatic`
4. Запустите через gunicorn или другой WSGI сервер

### Frontend
1. Соберите проект: `npm run build`
2. Разверните папку `build/` на статический хостинг

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан в рамках хакатона.

## 👥 Авторы

- **Федя** - Backend разработка
- **Родя** - Frontend разработка
- **Сережа** - ML/AI интеграция

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что переменные окружения настроены правильно
3. Проверьте логи сервера на наличие ошибок
