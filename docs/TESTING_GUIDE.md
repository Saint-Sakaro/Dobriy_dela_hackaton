# Инструкция по тестированию функционала портала "Добрые дела Росатома"

## Подготовка к тестированию

### 1. Запуск бэкенда
```bash
cd backend
source ../.venv/bin/activate  # или .venv\Scripts\activate на Windows
pip install -r ../requirements.txt  # если ещё не установлено
python manage.py migrate
python manage.py seed_demo  # создаст демо-данные
python manage.py runserver
```
Бэкенд будет доступен на `http://localhost:8000`

### 2. Запуск фронтенда
```bash
cd frontend
npm install  # если ещё не установлено
npm run dev
```
Фронтенд будет доступен на `http://localhost:5173`

### 3. Демо-аккаунты (создаются через `seed_demo`)
- **Обычный пользователь**: `demo_user` / `demo12345`
- **Модератор**: `demo_moderator` / `demo12345`

---

## Тестирование публичных страниц (без авторизации)

### ✅ Главная страница (`/`)
1. Откройте `http://localhost:5173`
2. **Проверьте:**
   - Отображается заголовок "Добрые дела Росатома"
   - Есть выбор города в хедере
   - При смене города контент обновляется
   - Есть навигация: НКО, База знаний, Календарь, Новости

### ✅ Список НКО (`/ngo`)
1. Перейдите на страницу НКО
2. **Проверьте:**
   - Отображается список НКО
   - Работает фильтр по категориям
   - Работает поиск по названию
   - При выборе города показываются только НКО этого города
   - Можно кликнуть на НКО и перейти на детальную страницу

### ✅ Страница НКО (`/ngo/:slug`)
1. Откройте любую НКО из списка
2. **Проверьте:**
   - Отображается название, описание, категории
   - Показывается город и регион
   - Есть контактная информация (если заполнена)
   - **Карта Яндекс.Карт** отображается (показывает город НКО)
   - Кнопка "Сохранить" требует авторизации

### ✅ База знаний (`/knowledge`)
1. Перейдите в раздел "База знаний"
2. **Проверьте:**
   - Отображается список материалов
   - Работает поиск по названию/описанию
   - Фильтры по категориям и типам (видео/документ/статья)
   - Можно открыть материал в модальном окне
   - Для видео показывается плеер
   - Для документов есть кнопка скачивания

### ✅ Календарь событий (`/calendar`)
1. Перейдите в календарь
2. **Проверьте:**
   - События сгруппированы по месяцам
   - Работает фильтр по категориям
   - При выборе города показываются только события этого города
   - Кнопки "Участвовать" и "Избранное" требуют авторизации
   - Есть форма создания события (требует авторизации)

### ✅ Новости (`/news`)
1. Перейдите в раздел новостей
2. **Проверьте:**
   - Отображается список новостей
   - Работает фильтр по категориям
   - При выборе города показываются новости этого города
   - Можно открыть новость и прочитать полный текст
   - Кнопка "Сохранить" требует авторизации

---

## Тестирование авторизации

### ✅ Регистрация
1. Перейдите в `/profile`
2. Выберите вкладку "Регистрация"
3. Заполните форму:
   - Имя пользователя
   - Email
   - Пароль (минимум 8 символов)
   - Имя, фамилия (необязательно)
   - Город (необязательно)
4. Нажмите "Зарегистрироваться"
5. **Проверьте:**
   - Вы автоматически вошли в систему
   - Токен сохранён в localStorage
   - Отображается ваш профиль

### ✅ Вход через логин/пароль
1. В форме входа введите: `demo_user` / `demo12345`
2. Нажмите "Войти"
3. **Проверьте:**
   - Вы вошли в систему
   - Отображается профиль пользователя

### ✅ Вход через VK ID
1. В форме входа нажмите "Войти через VK ID"
2. **Проверьте:**
   - Открывается окно авторизации VK
   - После авторизации вы входите в систему
   - Создаётся аккаунт с данными из VK (если первый раз)
   - При повторном входе используется существующий аккаунт

**Примечание:** Для работы VK ID нужно:
- Получить App ID на https://dev.vk.com/
- Добавить в `.env.development`: `VITE_VK_APP_ID=ваш_app_id`
- Указать redirect URL в настройках приложения VK

---

## Тестирование личного кабинета

### ✅ Избранное
1. Войдите в систему
2. Перейдите на страницу любой НКО
3. Нажмите "Сохранить"
4. **Проверьте:**
   - НКО добавлена в избранное
   - В профиле → "Избранное" отображается сохранённая НКО
   - Можно удалить из избранного

5. Повторите для событий и новостей
6. **Проверьте:**
   - Все типы материалов сохраняются
   - Отображаются правильные ссылки для перехода

### ✅ Регистрация на события
1. Войдите в систему
2. Перейдите в календарь событий
3. Нажмите "Участвовать" на любом событии
4. **Проверьте:**
   - Кнопка меняется на "Отменить участие"
   - В профиле → "Мои события" отображается запись
   - Можно отменить участие

### ✅ Создание НКО
1. В профиле перейдите на вкладку "Моя НКО"
2. Нажмите "Создать НКО"
3. Заполните форму:
   - Название (обязательно)
   - Город (обязательно)
   - Описание (обязательно)
   - Категории (необязательно)
   - Контакты (необязательно)
4. Нажмите "Создать черновик"
5. **Проверьте:**
   - НКО создана со статусом "Черновик"
   - Отображается в списке "Мои организации"
   - Есть кнопка "Отправить на модерацию"
6. Нажмите "Отправить на модерацию"
7. **Проверьте:**
   - Статус меняется на "На модерации"
   - НКО видна модератору в админ-панели

### ✅ Создание события
1. Войдите в систему
2. Перейдите в календарь
3. Прокрутите вниз до блока "Организуете мероприятие?"
4. Нажмите "Добавить событие"
5. Заполните форму:
   - Название (обязательно)
   - Дата и время начала (обязательно)
   - Город (обязательно)
   - Описание, место, категория (необязательно)
6. Нажмите "Создать"
7. **Проверьте:**
   - Событие создано со статусом "Черновик"
   - Отображается в календаре (только для создателя)
   - Отправлено на модерацию

### ✅ Редактирование профиля
1. В профиле перейдите на вкладку "Настройки"
2. Измените данные:
   - Имя, фамилия
   - Email
   - Телефон
   - Город
3. Нажмите "Сохранить изменения"
4. **Проверьте:**
   - Данные обновлены
   - Отображается сообщение об успехе
   - Изменения видны в профиле

---

## Тестирование админ-панели

### ✅ Вход в админ-панель
1. Войдите как модератор: `demo_moderator` / `demo12345`
2. Перейдите на `/admin`
3. **Проверьте:**
   - Отображается панель администратора
   - Видны статистики: НКО на модерации, события, новости, материалы

### ✅ Модерация НКО
1. В админ-панели найдите раздел "НКО на модерации"
2. **Проверьте:**
   - Отображаются НКО со статусом "pending"
   - Видна информация о НКО: название, город, категории, описание
3. Нажмите "Одобрить" на одной НКО
4. **Проверьте:**
   - НКО исчезает из списка на модерации
   - Статус меняется на "published"
   - НКО видна в публичном списке
5. Нажмите "Отклонить" на другой НКО
6. **Проверьте:**
   - НКО исчезает из списка
   - Статус меняется на "rejected"
   - НКО не видна в публичном списке

### ✅ Модерация событий
1. В админ-панели найдите раздел "События на модерации"
2. **Проверьте:**
   - Отображаются события со статусом "pending"
3. Одобрите или отклоните событие
4. **Проверьте:**
   - Событие обновляется корректно
   - Одобренные события видны в календаре

### ✅ Создание новости
1. В админ-панели найдите раздел "Новости"
2. Нажмите "Добавить новость"
3. Заполните форму:
   - Заголовок
   - Категория
   - Краткое описание
   - Полный текст
   - Город (необязательно)
   - Обложка (URL)
4. Нажмите "Опубликовать"
5. **Проверьте:**
   - Новость сохранена
   - Отображается в списке новостей
   - Видна на публичной странице новостей

### ✅ Создание материала в базе знаний
1. В админ-панели найдите раздел "База знаний"
2. Нажмите "Добавить материал"
3. Заполните форму:
   - Название
   - Тип (видео/документ/статья)
   - Категории
   - Краткое описание
   - Полный текст
   - Ссылка на файл/видео
   - Обложка (URL)
4. Нажмите "Опубликовать"
5. **Проверьте:**
   - Материал сохранён
   - Отображается в базе знаний
   - Виден на публичной странице

---

## Тестирование фильтров и поиска

### ✅ Фильтрация по городу
1. Выберите город в хедере (например, "Саров")
2. **Проверьте на всех страницах:**
   - Список НКО: показываются только НКО этого города
   - Календарь: показываются только события этого города
   - Новости: показываются новости этого города (или общие)

### ✅ Фильтрация по категориям
1. На странице НКО выберите категорию
2. **Проверьте:**
   - Список фильтруется по выбранной категории
3. Повторите для календаря и новостей

### ✅ Поиск
1. На странице НКО введите запрос в поиск
2. **Проверьте:**
   - Результаты фильтруются по названию
   - Поиск работает в реальном времени

---

## Тестирование карт

### ✅ Яндекс.Карты на странице НКО
1. Откройте любую НКО
2. Прокрутите до раздела "Расположение"
3. **Проверьте:**
   - Отображается карта Яндекс.Карт
   - Показывается город НКО
   - Есть маркер с названием НКО
   - Карта интерактивна (можно масштабировать, перемещать)

**Примечание:** Для работы карт нужно:
- Получить API ключ на https://developer.tech.yandex.ru/
- Заменить `YOUR_YANDEX_MAPS_API_KEY` в `frontend/index.html` на реальный ключ

---

## Тестирование граничных случаев

### ✅ Обработка ошибок
1. Попробуйте войти с неверными данными
2. **Проверьте:**
   - Отображается сообщение об ошибке
3. Попробуйте создать НКО без обязательных полей
4. **Проверьте:**
   - Отображается валидационная ошибка

### ✅ Пустые состояния
1. Создайте новый аккаунт
2. **Проверьте:**
   - В избранном: "Избранное пока пусто"
   - В событиях: "Вы ещё не записались"
   - В НКО: "У вас ещё нет добавленных НКО"

### ✅ Права доступа
1. Войдите как обычный пользователь
2. Попробуйте открыть `/admin`
3. **Проверьте:**
   - Отображается сообщение "Недостаточно прав"
4. Войдите как модератор
5. **Проверьте:**
   - Доступ к админ-панели открыт

---

## Чеклист полного тестирования

- [ ] Главная страница и выбор города
- [ ] Список НКО с фильтрами
- [ ] Страница НКО с картой
- [ ] База знаний с материалами
- [ ] Календарь событий
- [ ] Новости
- [ ] Регистрация пользователя
- [ ] Вход через логин/пароль
- [ ] Вход через VK ID
- [ ] Добавление в избранное (НКО, события, новости)
- [ ] Регистрация на события
- [ ] Создание НКО
- [ ] Создание события
- [ ] Редактирование профиля
- [ ] Модерация НКО
- [ ] Модерация событий
- [ ] Создание новости (модератор)
- [ ] Создание материала (модератор)
- [ ] Фильтры по городу и категориям
- [ ] Поиск
- [ ] Карты Яндекс.Карт
- [ ] Обработка ошибок
- [ ] Пустые состояния
- [ ] Права доступа

---

## Известные ограничения

1. **VK ID**: Требует настройки App ID в VK Developer
2. **Яндекс.Карты**: Требует API ключ (можно получить бесплатно)
3. **RAG/GigaChat**: Пока не реализовано (заглушка)

---

## Полезные команды

### Очистка и пересоздание БД
```bash
cd backend
rm db.sqlite3
python manage.py migrate
python manage.py seed_demo
```

### Создание суперпользователя
```bash
cd backend
python manage.py createsuperuser
```

### Проверка API
```bash
# Список городов
curl http://localhost:8000/api/v1/cities/

# Список НКО
curl http://localhost:8000/api/v1/organizations/

# Вход (получить токен)
curl -X POST http://localhost:8000/api/v1/auth/login/ \
  -H "Content-Type: application/json" \
  -d '{"username":"demo_user","password":"demo12345"}'
```

---

**Удачного тестирования! 🚀**

